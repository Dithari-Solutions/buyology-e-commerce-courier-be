{
  "properties": [
    {
      "name": "cors.allowed-origins",
      "type": "java.lang.String",
      "description": "Comma-separated list of allowed CORS origins (e.g. https://app.buyology.com,https://ops.buyology.com)."
    },
    {
      "name": "outbox.poll-interval-ms",
      "type": "java.lang.Long",
      "description": "How often (in milliseconds) the outbox publisher job polls for PENDING events."
    },
    {
      "name": "location.retention-days",
      "type": "java.lang.Integer",
      "description": "Number of days to retain courier location history before the cleanup job purges old rows."
    },
    {
      "name": "location.cleanup-cron",
      "type": "java.lang.String",
      "description": "Cron expression for the location history cleanup job (Spring cron format)."
    },
    {
      "name": "auth.jwt.secret",
      "type": "java.lang.String",
      "description": "HMAC-SHA256 secret for signing and verifying courier-issued JWTs. Must be at least 32 characters. Override AUTH_JWT_SECRET in production."
    },
    {
      "name": "auth.jwt.issuer",
      "type": "java.lang.String",
      "description": "Issuer claim (iss) embedded in courier-issued JWTs. Used to route token validation away from Keycloak."
    },
    {
      "name": "auth.jwt.access-token-expiry-seconds",
      "type": "java.lang.Long",
      "description": "Access token TTL in seconds. Default: 900 (15 minutes)."
    },
    {
      "name": "auth.jwt.refresh-token-expiry-seconds",
      "type": "java.lang.Long",
      "description": "Refresh token TTL in seconds. Default: 2592000 (30 days)."
    },
    {
      "name": "auth.admin.jwt.audience",
      "type": "java.lang.String",
      "description": "Expected `aud` claim value in Keycloak admin JWTs. Tokens missing this audience are rejected. Must match the Audience mapper configured in Keycloak for the buyology-courier-service client."
    },
    {
      "name": "auth.refresh-token.cleanup-cron",
      "type": "java.lang.String",
      "defaultValue": "0 0 3 * * *",
      "description": "Cron expression for the nightly job that purges expired refresh tokens. Defaults to 03:00 server time."
    },
    {
      "name": "management.tracing.sampling.probability",
      "type": "java.lang.Double",
      "description": "Fraction of requests to sample for distributed tracing (0.0–1.0). Use 1.0 in dev, 0.1 in prod."
    },
    {
      "name": "notification.email.enabled",
      "type": "java.lang.Boolean",
      "defaultValue": false,
      "description": "Master switch for email notifications. Set to true and supply twilio.sendgrid.* credentials."
    },
    {
      "name": "twilio.sendgrid.api-key",
      "type": "java.lang.String",
      "description": "Twilio SendGrid API key (SG.xxx). Required when notification.email.enabled=true."
    },
    {
      "name": "twilio.sendgrid.from-email",
      "type": "java.lang.String",
      "defaultValue": "noreply@buyology.com",
      "description": "Verified sender email address shown in the From field of courier assignment emails."
    },
    {
      "name": "twilio.sendgrid.from-name",
      "type": "java.lang.String",
      "defaultValue": "Buyology Courier",
      "description": "Display name shown alongside the from-email in courier assignment emails."
    }
  ]
}
